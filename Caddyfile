{$DOMAIN:localhost} {
	# WebSocket endpoint for agent
	handle /ws {
		reverse_proxy agent:9800
	}

	# Everything else (including /relay WebSocket) goes to Next.js
	handle {
		reverse_proxy web:3000
	}
}
